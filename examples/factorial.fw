# factorial.fw — Recursive factorial
#
# Usage:
#   fwiz factorial(result=?, n=5)
#   result = 120
#
# Requires: conditions (: syntax) and recursion (formula calls to self)

result = 1 : n <= 0
result = n * factorial(result=?prev, n=n-1) : n > 0
